# Environment Variables for Alzheimer's Detection App
# Copy this file to .env for local development

# Flask Configuration
FLASK_APP=web_app/app.py
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
PORT=5000
HOST=0.0.0.0

# Model Configuration (optional - overrides config_local.yaml if needed)
# MODEL_PATH=./saved_model/test_run_model_low_loss.pth.tar
# CONFIG_PATH=config_local.yaml

# Upload Settings
MAX_UPLOAD_SIZE_MB=50
UPLOAD_FOLDER=uploads

# Production Settings (Render will override these)
# PYTHON_VERSION=3.11.0
